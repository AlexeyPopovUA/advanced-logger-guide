version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm run test
    build:
      commands:
        - npm run build-prod
  artifacts:
    baseDirectory: ./dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - /opt/google/**/*
